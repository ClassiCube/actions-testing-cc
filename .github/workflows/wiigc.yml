name: Build latest (Wii/GameCube)
on: [push]

concurrency:
  group: ${{ github.ref }}-wiigc
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkitppc:latest
    steps:
      - uses: actions/checkout@v3
      - name: compile
        run: |
          make wii
          make clean
          make gamecube

      - uses: ./.github/actions/notify_failure
        if: ${{ steps.compile.outputs.status == 'failure' }}
        with:
          NOTIFY_MESSAGE: 'Failed to compile Wii/Gamecube build'
          WEBHOOK_URL: '${{ secrets.WEBHOOK_URL }}'
          
      - uses: ./.github/actions/upload_build
        if: ${{ steps.compile.outputs.status == 'success' }}
        with:
          SOURCE_FILE: 'ClassiCube-Wii'
          DEST_NAME: 'latest/wii/ClassiCube'
          
      - uses: ./.github/actions/upload_build
        if: ${{ steps.compile.outputs.status == 'success' }}
        with:
          SOURCE_FILE: 'ClassiCube-GameCube'
          DEST_NAME: 'latest/gamecube/ClassiCube'