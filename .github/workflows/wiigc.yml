name: Build latest (Wii/GameCube)
on: [push]

concurrency:
  group: ${{ github.ref }}-wiigc
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkitppc:latest
    steps:
      - uses: actions/checkout@v3
      - name: Compile
        run: |
          make wii
          make clean
          make gamecube

  notify-failure:
    needs: [build]
    if: ${{ always() && (needs.build.result == 'failure') }}
    uses: ./.github/actions/notify_failure.yml
    with:
      WEBHOOK_MESSAGE: 'Failed to compile Wii/GameCube build'
    secrets:
      inherit: true