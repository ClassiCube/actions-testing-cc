name: Build latest (PSP)
on: [push]

concurrency:
  group: ${{ github.ref }}-psp
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: pspdev/pspdev:latest
    steps:
      - uses: actions/checkout@v3
      - name: Compile
        run: |
          make psp

  notify-failure:
    needs: [build]
    if: ${{ always() && (needs.build.result == 'failure') }}
    uses: ./.github/actions/notify_failure.yml
    with:
      WEBHOOK_MESSAGE: 'Failed to compile PSP build'
    secrets: inherit